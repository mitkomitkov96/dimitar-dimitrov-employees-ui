import { Routes } from '@angular/router';
import { EmployeesOverlapComponent } from './employees-overlap/employees-overlap.component';

export const routes: Routes = [
  {
    path: '',
    component: EmployeesOverlapComponent
  }
];
